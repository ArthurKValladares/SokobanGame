#pragma once

namespace sokoban::config {

// Player clip sources live in assets/manifest.json (animation entries with
// player animation roles).
inline constexpr float playerAnimationFadeSeconds = 0.12f;
inline constexpr float enemyFacingSlerpSeconds = 0.15f;

} // namespace sokoban::config
