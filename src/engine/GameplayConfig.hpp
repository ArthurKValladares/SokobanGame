#pragma once

namespace sokoban::config {

inline constexpr float stepDurationSeconds = 0.25f;
inline constexpr float minimumStepDurationSeconds = 0.05f;
inline constexpr float maximumStepDurationSeconds = 1.0f;

} // namespace sokoban::config
